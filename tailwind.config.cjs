const config = {
	content: ['./src/**/*.{html,js,svelte,ts}'],

	theme: {
		extend: {
			fontFamily: {
				sans: ['poppins', 'font-sans', 'helvetica']
			}
		}
	},

	plugins: []
};

module.exports = config;
